Instead of spreading the entire object returned by `useField`, I extracted the `reset` function and grouped the remaining input-specific properties (`value`, `type`, `onChange`) into a new object using the rest syntax in the previous excercise:

```javascript
const { reset: resetContent, ...contentProps } = useField('text')
