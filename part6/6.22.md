The notification-related context was already moved to its own file `NotificationContext.jsx` in a previous exercise. The file contains the context, the `NotificationContextProvider`, and the custom hook `useNotification` that encapsulates the notification logic. Components such as `Notification` and `App` call `useNotification` directly instead of using `useContext` separately.
